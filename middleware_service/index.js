
const hybridService = require("./hybridService");

/**
 * Middleware entry point.
 * This function is designed to be imported by the Express route handler.
 * It encapsulates the service logic.
 */
module.exports = hybridService;
